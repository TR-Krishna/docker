apiVersion: v1
kind: Service
metadata:
  name: apimeter-service
  namespace: microservices

spec: 
  selector:
    app: apimeter
  ports: 
    - port: 8080
      targetPort: 8080
      nodePort: 30000
  type : NodePort